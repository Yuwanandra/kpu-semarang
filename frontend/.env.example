# URL backend API. Saat development lokal, biarkan kosong (memakai proxy Vite ke localhost:4000).
# Saat production di Vercel, isi dengan URL backend, contoh:
VITE_API_URL=https://kpu-semarang-api.vercel.app/api
